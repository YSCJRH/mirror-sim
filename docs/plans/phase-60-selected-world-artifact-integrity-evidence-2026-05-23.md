# Phase 60 Selected-World Review Artifact Integrity Evidence

Date: 2026-05-23

Issue: `#467` `Phase 60: add selected-world review artifact integrity smoke`

Current work item: `#467` `Phase 60: add selected-world review artifact integrity smoke`

This note records the tracked Phase 60 evidence slice for selected-world review artifact
integrity. The slice keeps the selected bounded fictional world set fixed to:

- `fog-harbor-east-gate`
- `museum-night`
- `library-rain`

The evidence is generated by `scripts/smoke_phase60_selected_world_artifact_integrity.py`
after the deterministic `eval-transfer` pipeline materializes the current artifacts.
Run `python scripts/smoke_phase60_selected_world_artifact_integrity.py` to reproduce the
selected-world review artifact integrity evidence locally.
Focused validation is `python -m pytest backend/tests/test_phase60_selected_world_artifact_integrity.py -q`;
phase validation includes `./make.ps1 eval-transfer`, `./make.ps1 smoke`, and `./make.ps1 test`.

## Artifact Roots

- `fog-harbor-east-gate`
  - expected root: `artifacts/demo`
  - eval summary: `artifacts/demo/eval/summary.json`
- `museum-night`
  - expected root: `artifacts/worlds/museum-night`
  - eval summary: `artifacts/worlds/museum-night/eval/summary.json`
- `library-rain`
  - expected root: `artifacts/worlds/library-rain`
  - eval summary: `artifacts/worlds/library-rain/eval/summary.json`
- aggregate transfer summary: `artifacts/transfer/summary.json`

## Integrity Checks

- Stable selected world ids match `fog-harbor-east-gate`, `museum-night`, and `library-rain`.
- Each selected world keeps the expected artifact root and eval summary path.
- Each selected world eval summary reports `status: pass`.
- Every report claim keeps both `label` and `evidence_ids`.
- `claim_evidence_resolves` verifies that each report claim evidence id resolves to an ingested chunk id.
- The aggregate transfer summary preserves:
  - `world_count: 3`
  - `scenario_count: 8`
  - `tracked_outcome_count: 18`
  - `transfer_worlds_with_default_report_delta: 3`
  - `transfer_proof_world_local: true`

## Boundary

This evidence slice does not start sessions, does not generate branches, does not roll back
sessions, does not create worlds, and does not call provider/model paths.

It does not change scenario DSL, claim labels, run trace shape, compare artifact shape, session/node manifest shape, public demo artifact layout, or plugin MCP contract.

It does not promote private-beta-wide readiness, readiness for unreviewed worlds, launch-hub
delivery, Hosted GPT/BYOK operation, storage/auth expansion, or runtime mutation expansion.

## Validation Commands

```powershell
python scripts/smoke_phase60_selected_world_artifact_integrity.py
python -m pytest backend/tests/test_phase60_selected_world_artifact_integrity.py -q
python scripts/check_no_secrets.py
./make.ps1 eval-transfer
./make.ps1 smoke
./make.ps1 test
```
